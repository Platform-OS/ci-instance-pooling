{
  "scripts": {
    "api-tests": "playwright test --project=api-tests",
    "e2e-tests": "playwright test --project=e2e-tests"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.1",
    "@types/node": "^22.14.0"
  }
}
