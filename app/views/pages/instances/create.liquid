---
method: post
layout: instances
slug: instances
---

{% liquid
  include 'require_auth'
  function object = 'lib/commands/instances/create', instance: context.params.instance

  if object.valid
    redirect_to '/' 
  else
    render 'instances/new', object: object
  endif
%}