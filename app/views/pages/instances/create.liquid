---
method: post
layout: instances
slug: instances
---

{% liquid
  include 'require_auth'
  function object = 'lib/commands/instances/create', instance: context.params.instance

  if object.valid
    redirect_to '/' 
  else
    theme_render_rc 'layout/new', errors: object.errors, domain: context.params.domain, pos_cli_token: context.params.pos_cli_token
  endif
%}