---
layout: instances
slug: /
---

{% liquid 
  include 'require_auth'

  function profile = 'modules/user/helpers/current_profile'
    
  function records = 'lib/queries/instances/search'
  assign results = records.results
%}

{% render 'instances/index', results: results, profile: profile %}