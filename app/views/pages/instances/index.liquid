---
layout: instances
slug: /
---

{% liquid 
    include 'require_auth'

    function records = 'lib/queries/instances/search'
    assign results = records.results
    %}

{% render 'instances/index', results: results %}