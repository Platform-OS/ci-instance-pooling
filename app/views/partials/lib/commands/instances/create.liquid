{% liquid 
  function object = 'lib/commands/instances/create/build', domain: domain, pos_cli_token: pos_cli_token, available: true
  function object = 'lib/commands/instances/create/check', object: object  

  if object.valid 
    function object = 'lib/commands/instances/create/execute', object: object
  endif

  return object
%}